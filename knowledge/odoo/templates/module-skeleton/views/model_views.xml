<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ==================== VUE FORMULAIRE ==================== -->
    <record id="view_{{model_name}}_form" model="ir.ui.view">
        <field name="name">{{model.name}}.form</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <form string="{{Model Name}}">
                <header>
                    <button name="action_confirm"
                            string="Confirmer"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_done"
                            string="Terminer"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'confirmed'"/>
                    <button name="action_cancel"
                            string="Annuler"
                            type="object"
                            invisible="state in ('cancelled', 'done')"/>
                    <button name="action_draft"
                            string="Remettre en brouillon"
                            type="object"
                            invisible="state != 'cancelled'"/>
                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="draft,confirmed,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <!-- Boutons statistiques -->
                    </div>
                    <widget name="web_ribbon"
                            title="Archivé"
                            bg_color="bg-danger"
                            invisible="active"/>
                    <div class="oe_title">
                        <h1>
                            <field name="name"
                                   placeholder="Nouveau..."
                                   readonly="state != 'draft'"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Informations générales">
                            <field name="partner_id"/>
                            <field name="date"/>
                            <field name="user_id"/>
                        </group>
                        <group string="Montants">
                            <field name="currency_id" invisible="1"/>
                            <field name="amount_total"/>
                            <field name="company_id"
                                   groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Lignes" name="lines">
                            <field name="line_ids"
                                   readonly="state != 'draft'">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="product_id"/>
                                    <field name="name"/>
                                    <field name="quantity"/>
                                    <field name="price_unit"/>
                                    <field name="currency_id" column_invisible="1"/>
                                    <field name="subtotal"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="description"
                                   placeholder="Notes internes..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ==================== VUE LISTE ==================== -->
    <record id="view_{{model_name}}_tree" model="ir.ui.view">
        <field name="name">{{model.name}}.tree</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <tree string="{{Model Name}}"
                  decoration-danger="state == 'cancelled'"
                  decoration-success="state == 'done'"
                  decoration-muted="not active"
                  multi_edit="1">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="date"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="amount_total" sum="Total"/>
                <field name="state"
                       widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'confirmed'"
                       decoration-success="state == 'done'"
                       decoration-danger="state == 'cancelled'"/>
                <field name="company_id"
                       groups="base.group_multi_company"/>
                <field name="active" column_invisible="1"/>
            </tree>
        </field>
    </record>

    <!-- ==================== VUE RECHERCHE ==================== -->
    <record id="view_{{model_name}}_search" model="ir.ui.view">
        <field name="name">{{model.name}}.search</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <search string="Rechercher">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="user_id"/>
                <separator/>
                <filter string="Brouillons"
                        name="draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter string="Confirmés"
                        name="confirmed"
                        domain="[('state', '=', 'confirmed')]"/>
                <filter string="Terminés"
                        name="done"
                        domain="[('state', '=', 'done')]"/>
                <separator/>
                <filter string="Mes enregistrements"
                        name="my_records"
                        domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter string="Archivés"
                        name="inactive"
                        domain="[('active', '=', False)]"/>
                <group expand="0" string="Grouper par">
                    <filter string="Client"
                            name="groupby_partner"
                            context="{'group_by': 'partner_id'}"/>
                    <filter string="État"
                            name="groupby_state"
                            context="{'group_by': 'state'}"/>
                    <filter string="Responsable"
                            name="groupby_user"
                            context="{'group_by': 'user_id'}"/>
                    <filter string="Date"
                            name="groupby_date"
                            context="{'group_by': 'date:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ==================== VUE KANBAN ==================== -->
    <record id="view_{{model_name}}_kanban" model="ir.ui.view">
        <field name="name">{{model.name}}.kanban</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state"
                    class="o_kanban_mobile"
                    quick_create="false">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="amount_total"/>
                <field name="state"/>
                <field name="user_id"/>
                <field name="currency_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                    <div class="o_kanban_record_top_right">
                                        <field name="state"
                                               widget="label_selection"
                                               options="{'classes': {
                                                   'draft': 'info',
                                                   'confirmed': 'warning',
                                                   'done': 'success',
                                                   'cancelled': 'danger'
                                               }}"/>
                                    </div>
                                </div>
                                <div class="o_kanban_record_body">
                                    <field name="partner_id"/>
                                    <div class="mt-2">
                                        <strong>
                                            <field name="amount_total"
                                                   widget="monetary"
                                                   options="{'currency_field': 'currency_id'}"/>
                                        </strong>
                                    </div>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left"/>
                                    <div class="oe_kanban_bottom_right">
                                        <field name="user_id"
                                               widget="many2one_avatar_user"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ==================== VUE CALENDRIER ==================== -->
    <record id="view_{{model_name}}_calendar" model="ir.ui.view">
        <field name="name">{{model.name}}.calendar</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <calendar string="{{Model Name}}"
                      date_start="date"
                      color="state"
                      quick_add="false">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="amount_total"/>
            </calendar>
        </field>
    </record>

    <!-- ==================== VUE PIVOT ==================== -->
    <record id="view_{{model_name}}_pivot" model="ir.ui.view">
        <field name="name">{{model.name}}.pivot</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <pivot string="{{Model Name}}">
                <field name="date" type="row" interval="month"/>
                <field name="state" type="col"/>
                <field name="amount_total" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- ==================== VUE GRAPHIQUE ==================== -->
    <record id="view_{{model_name}}_graph" model="ir.ui.view">
        <field name="name">{{model.name}}.graph</field>
        <field name="model">{{model.name}}</field>
        <field name="arch" type="xml">
            <graph string="{{Model Name}}" type="bar">
                <field name="date" interval="month"/>
                <field name="amount_total" type="measure"/>
            </graph>
        </field>
    </record>

    <!-- ==================== ACTION ==================== -->
    <record id="action_{{model_name}}" model="ir.actions.act_window">
        <field name="name">{{Model Name}}</field>
        <field name="res_model">{{model.name}}</field>
        <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
        <field name="search_view_id" ref="view_{{model_name}}_search"/>
        <field name="context">{'search_default_draft': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Créer votre premier enregistrement
            </p>
            <p>
                Cliquez sur le bouton "Créer" pour commencer.
            </p>
        </field>
    </record>

</odoo>
