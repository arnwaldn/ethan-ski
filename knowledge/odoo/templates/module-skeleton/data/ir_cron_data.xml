<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron Job: Synchronisation périodique -->
        <record id="ir_cron_{{module_name}}_sync" model="ir.cron">
            <field name="name">{{Module Name}}: Synchronisation</field>
            <field name="model_id" ref="model_{{model_name_underscore}}"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync()</field>
            <field name="interval_number">5</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="doall">False</field>
        </record>

        <!-- Cron Job: Nettoyage des logs -->
        <record id="ir_cron_{{module_name}}_cleanup" model="ir.cron">
            <field name="name">{{Module Name}}: Nettoyage des logs</field>
            <field name="model_id" ref="model_{{module_name}}_log"/>
            <field name="state">code</field>
            <field name="code">model.cron_cleanup_old_logs(days=90)</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="doall">False</field>
        </record>

    </data>
</odoo>
